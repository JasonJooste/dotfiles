# Settings shared by scripts across tiers and machines. Tracked (and public), so nothing secret goes here.

# Tailnet name of the always-on server (SSH, backups)
SERVER=server
# Where the backup drive is mounted on the server
BACKUP_DRIVE=/mnt/tosh
