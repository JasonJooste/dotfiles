[Unit]
Description=Daily check of the server's alerts

[Timer]
OnCalendar=daily
# Catch up on wake/boot if the laptop was off or asleep at the scheduled time
Persistent=true

[Install]
WantedBy=timers.target
